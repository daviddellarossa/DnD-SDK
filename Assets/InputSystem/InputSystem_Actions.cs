// This is a representation as I don't have the exact code
// Only add this if the MousePosition action doesn't already exist
// Look for MousePosition in your existing input actions
// If it doesn't exist, add it through the Input System window
